// Exporting the module configuration
module.exports = {
  // Using the Docusaurus Babel preset for transpilation
  presets: [require.resolve('@docusaurus/core/lib/babel/preset')],
  // Enabling the "compact" option to minimize the output code
  compact: true,
};
